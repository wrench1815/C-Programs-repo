#include <stdio.h>
#include <stdlib.h>

int main()
{
/**< To print the numbers from 0 to 10 with the help of while loop */
    int count;

    count=0;
    while(count<=10)
    {
        printf("%d ",count);
        count++;
    }
    return 0;
}
