#include <stdio.h>
#include <stdlib.h>

int main()
{
/**< to print numbers 0 to 10 with the help of for loop */
    int count;

    for(count=0; count<11; count++)
    {
        printf("%d ",count);
    }
    return 0;
}
